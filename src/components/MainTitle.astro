---
interface Props {
	title: string;
}

const { title } = Astro.props;
import downloadIcon from "../icons/download.svg";
---				

<section class="flex justify-between items-center ml-8 mt-14">
    <h1 class="text-white text-4xl font-semibold">{title}</h1>
    <button class="bg-accentLight rounded-md px-2 py-2 mr-8 flex items-center gap-2 hover:brightness-125">
        <img src={downloadIcon.src} alt="icon" width={"20px"} height={"20px"}/>
        <span class="text-white text-sm">COLA DE INSTALACIÓN</span>
    </button>
</section>
